import { component$ } from "@builder.io/qwik";

export default component$(() => (
  <span class="self-center md:text-base text-gray-800 whitespace-nowrap dark:text-white flex items-center">
    Home
  </span>
));
