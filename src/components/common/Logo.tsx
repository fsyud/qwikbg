import { component$ } from "@builder.io/qwik";

export default component$(() => (
  <span class="self-center text-2xl md:text-xl text-gray-800 whitespace-nowrap dark:text-white flex items-center font-mon">
    ✏️
  </span>
));
